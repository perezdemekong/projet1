export interface Speciality {
    id: number;
    name: string;
    code: string;
    status: boolean;
}

export interface SpecialityData {
    name: string;
    code: string;
    status: boolean;
}

export interface SpecialityStatus {
    status: boolean;
}